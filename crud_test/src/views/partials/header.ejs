<header class="header">
  <div class="logo">
    <img src="https://files.catbox.moe/tyeedc.png" alt="Note&Neko Logo" class="logo-image" />
    <span class="logo-text">Note&Neko</span>
  </div>
  <nav class="nav">
    <a href="/">Inicio</a>
    <a href="/products">Productos</a>
    <a href="/categories">Categorías</a>
    <a href="/comunidad">Comunidad</a>
  </nav>
  <div class="icons">
    <span class="icon-envelope"></span>
    <a href="/cart" class="cart-icon">
      <span class="icon-cart">🛒</span>
      <% if (cart && Array.isArray(cart) && cart.length > 0) { %>
        <span class="cart-count"><%= cart.length %></span>
      <% } %>
    </a>
    <% if (user) { %>
      <div class="user-menu">
        <span class="user-name"><%= user.nombre %></span>
        <div class="user-dropdown">
          <a href="/auth/profile">Mi Perfil</a>
          <% if (user.rol === 'admin') { %>
            <a href="/admin">Admin</a>
          <% } %>
          <a href="/auth/logout">Cerrar Sesión</a>
        </div>
      </div>
    <% } else { %>
      <div class="auth-links">
        <a href="/auth/login" class="auth-link">Iniciar Sesión</a>
        <a href="/auth/signup" class="auth-link">Registrarse</a>
      </div>
    <% } %>
  </div>
</header> 